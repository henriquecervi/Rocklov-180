pipeline {
    agent {
        docker {
            image 'ruby'
        }
    }

    stages {
        stage('Preparando Ambiente') {
            steps {
                sh 'cd api && bundle install'
            }
        }
        stage('Testando') {
            steps {
                sh 'cd api && rspec'
            }
        }
    }
}